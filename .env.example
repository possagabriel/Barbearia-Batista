# Chave pública da Google Maps JavaScript API.
# Copie este arquivo para `.env` e preencha o valor.
# Ela é consumida via import.meta.env em builds Vite ou exposta como global
# window.VITE_GOOGLE_MAPS_API_KEY antes de js/config.js (ver README.md).
VITE_GOOGLE_MAPS_API_KEY=